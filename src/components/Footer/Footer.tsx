import React from "react";
import classes from "./footer.module.css";

const Footer = () => {
  return (
    <footer className={classes.wrapper}>
      <div className={classes.footer}>
        <h3>Developer Junaid</h3>
      </div>
    </footer>
  );
};

export default Footer;
